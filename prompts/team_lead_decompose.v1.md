# TeamLead Decompose Prompt

Convert the mission and answers into small TaskCards with clear attach points.
Keep the worker roles limited to builder and critic unless a verifier is required.

Each TaskCard must include a concrete title, goal, expected output, attach point, merge key, topology hint, and dependencies.
For handoff work, split scope/files/expected changes from safety/verification/report format.
