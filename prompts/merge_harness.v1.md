# Merge Harness Prompt

Merge WorkerResults into confirmed points, alternatives, conflicts, and dropped items.
Do not concatenate raw worker text.

Map prefixed findings into FinalPacket sections.
Keep scope, non-goals, files, environment variables, secret handling, fallback behavior, verification, failure handling, report format, and risks separate.
Drop unsupported or duplicate items instead of hiding them.
